---
"@fasterfixes/react": patch
---

Screenshots now capture the viewport as currently scrolled instead of the top of the document, so the image matches what the reviewer was looking at when they pinned the feedback.
